# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Distribution / packaging
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.pytest_cache/

# Django/Flask
db.sqlite3
*.log
instance/

# Environments
.env
.venv
env/
venv/

# VS Code (keep settings.json)
.vscode/*
!.vscode/settings.json

# ignore old code
old/

# Claude Code project instructions (internal notes — keep out of the public repo)
CLAUDE.md
claude.md

# MuJoCo runtime files
MJDATA.TXT
mjmodel.mjb

# SolidWorks / Office autosave-lock temp files
~$*

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes
.fseventsd

# Trained models and weights
# NOTE: un-ignore these before final submission so readers can run without retraining
#   (remove the lines below and update requirements.txt — see CLAUDE.md pre-publication checklist)
controllers/ppo/models/*
!controllers/ppo/models/.gitkeep
*.zip

# Training artifacts
controllers/ppo/tb_logs/*
!controllers/ppo/tb_logs/.gitkeep

# Recorded rollout videos (large binaries; upload into the README directly instead)
*.mp4

# Per-version eval outputs are ignored by default; the published best model (v41)
# and the cross-version summary are whitelisted below.
controllers/ppo/ppo_eval/*
!controllers/ppo/ppo_eval/.gitkeep
!controllers/ppo/ppo_eval/how-to-read-outputs.md
!controllers/ppo/ppo_eval/summary.csv

# --- Published v41 (best model) artifacts: weights, metrics, trajectories ---
# Un-ignore only these. The rollout .mp4 (upload into the README directly) and
# v41-notes.md (internal) intentionally stay ignored.
!controllers/ppo/models/dog_ppo_v41.zip
!controllers/ppo/ppo_eval/v41/
controllers/ppo/ppo_eval/v41/*
!controllers/ppo/ppo_eval/v41/metrics.json
!controllers/ppo/ppo_eval/v41/trajectory_100_runs_v41.png
!controllers/ppo/ppo_eval/v41/trajectory_ten_runs_v41.png
!controllers/ppo/ppo_eval/v41/v41_rollout_nonoise.gif
